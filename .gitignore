# build artifacts
build/
*.app/

# Parakeet models — large (1.4GB+), fetched by build.sh
models/

# runtime + distributables
wispr.log
elaborate.log
*.dmg
*.zip

# Sparkle framework + generated keys — fetched by setup-sparkle.sh
Frameworks/

# code-signing cert — machine-specific, regenerated locally (see README)
*.pem
*.p12
*.key

# macOS
.DS_Store

# parakeet.cpp build tree (cloned + built locally)
parakeet.cpp/
parakeet-env/

.codex-tmp/
