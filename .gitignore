# build artifacts
build/
*.app/

# whisper models — large (1.5GB+), fetched by build.sh
models/

# runtime
wispr.log

# code-signing cert — machine-specific, regenerated locally (see README)
*.pem
*.p12
*.key

# macOS
.DS_Store
